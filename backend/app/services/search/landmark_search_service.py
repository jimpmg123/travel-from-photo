# Removed in the signal-fusion refactor.
# Landmark detection is now one of N parallel signals collected by
# services/search/signal_collector_service.py. The cascade-era "this single
# helper decides the location" wrapper no longer fits the fusion flow.
