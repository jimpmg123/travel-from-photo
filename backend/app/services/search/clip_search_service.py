# Removed in the tiered signal-fusion refactor (2026-05-26).
# CLIP gate / scene hints were a cascade-era heuristic. The tiered flow
# replaces them with parallel signal collection (OCR + Landmark + Web + Logo)
# and GPT (Method A) reasoning. No caller imports this anymore.
