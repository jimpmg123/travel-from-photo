# Replaced by app/models/uploaded_image.py (split into UploadedImage +
# ImageExifMetadata as part of the Search signal-fusion schema).
# This module is intentionally empty and no longer imported anywhere.
